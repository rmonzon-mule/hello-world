# k8s

This directory is intended to hold Kubernetes manifests and related deployment/configuration files for the hello-world application.

Place files like `deployment.yaml`, `service.yaml`, `configmap.yaml`, or Helm charts here.
